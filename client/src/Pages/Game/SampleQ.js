function Sample (type, question, answerOptions, correctAnswer) {
    type = this.type;
    question = this.question;
    answerOptions = this.answerOptions;
    correctAnswer = this.correctAnswer;
}

module.exports = Sample;